sonar.projectKey=NafNawal04_to-do
sonar.organization=nafnawal04


# This is the name and version displayed in the SonarCloud UI.
#sonar.projectName=to-do
#sonar.projectVersion=1.0


# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
#sonar.sources=.

# maintenance/ holds teaching/tooling artifacts for this maintenance-simulation
# exercise, including intentional duplicate before/after source snapshots fed
# to Joern for CPG generation - not production code, so exclude it from
# analysis (it was inflating the Duplications metric).
sonar.exclusions=maintenance/**

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8